<div class="row center-xs">
	<div class="col-xs-12 col-sm-8 col-lg-6">
		<div class="box">
			<div class="white mdl-shadow--2dp">
				<div class="big-padding text-left cover relative">
					<%= image_tag @company.cover.url(:medium)%>
				</div>
			</div>
		</div>
	</div>
</div>



<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @company.name %>
</p>

<p>
  <strong>Bio:</strong>
  <%= @company.bio %>
</p>

<p>
  <strong>Email:</strong>
  <%= @company.email %>
</p>

<%= link_to 'Edit', edit_company_path(@company) %> |
<%= link_to 'Back', companies_path %>
