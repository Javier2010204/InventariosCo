<%= form_with(model: sale, local: true) do |form| %>
  <% if sale.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale.errors.count, "error") %> prohibited this sale from being saved:</h2>

      <ul>
      <% sale.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number, "Numero" %>
    <%= form.number_field :number %>
  </div>

  <div class="field">
    <%= form.label :client_id %>
    <%= form.collection_select :client_id, @clients,:id,:name, include_blank: false %>
  </div>

  <div class="field">
    <p>empresa</p>
    <p>usuario</p>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= link_to 'Agregar detalle', new_sale_sale_detail_path(sale_id: @sale.id) %>


<div class="">
    <h4>TABLA</h4>
    <%= (@sale.sale_details.count > 0) ? render(partial: 'table', locals: {sale: @sale, show_actions: true}) : 'No hay detalles cargados' %>
</div>
